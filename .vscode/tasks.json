{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-app",
            "type": "shell",
            "command": "lsof -i :8798 || (export WS_PORT=8798 && npm run start)",
            "runOptions": {
                "runOn": "folderOpen"
            },
        }
    ]
}
